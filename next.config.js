/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  trailingSlash: true,
  images: {
    domains: ["https://lh3.googleusercontent"],
  },
};

module.exports = nextConfig;
